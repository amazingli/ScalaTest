package day1

object Test4 {
  def main(args: Array[String]): Unit = {
    /**
     * 运算符
     * 算术运算符: + - * / %
     * 关系运算符: == != > < >= <=.
     * 与或非: && || !
     */
    var a, b, c, d, e, f = 0
    a = 1
    e = 1
    println("a+b的值:" + (a + b))
    println("a-b的值:" + (a - c))
    println("a*b的值:" + (a * d))
    println("a/b的值:" + (a / e))
    println("a%b的值:" + (a % f))

  }
}
